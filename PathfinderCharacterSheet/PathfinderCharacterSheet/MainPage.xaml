<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PathfinderCharacterSheet"
             x:Class="PathfinderCharacterSheet.MainPage"
             Title="Characters">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="Add_Clicked"></ToolbarItem>
    </ContentPage.ToolbarItems>
    <StackLayout>
        <ListView x:Name="Characters" SelectionMode="None" ItemTapped="Characters_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout Orientation="Horizontal" Margin="10">
                                <Label Text="{Binding Name}" TextColor="black" FontSize="Medium" HorizontalOptions="FillAndExpand" />
                                <Label Text="{Binding Race}" TextColor="black" FontSize="Medium" HorizontalOptions="EndAndExpand" />
                                <Label Text="{Binding TotalLevel}" TextColor="black" FontSize="Medium" HorizontalOptions="EndAndExpand" />
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
